apiVersion: 1

providers:
  - name: pg-index-check
    orgId: 1
    folder: "PostgreSQL Index Health"
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
